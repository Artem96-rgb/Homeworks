// Запросите у пользователя длину стороны квадрата и выведите периметр такого квадрата.

// let side;

// side = prompt('Введите сторону квадрата в метрах');

// let perimetr;

// perimetr = ((+side) + (+side) + (+side) + (+side));

// alert(`Периметр=${perimetr}`);